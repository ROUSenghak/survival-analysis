# GENERATED FILE -- do not hand-edit.
# Written by scripts/write_v0_4_scenario_config.py from
# reports/tables/synthetic_benchmark/v0_4_population_alias_revision/calibration/observable_targets.json (EMPIRICAL_OBSERVABLE, calibration buyers only)
# and reports/tables/synthetic_benchmark/v0_4_population_alias_revision/calibration/mechanism_parameters.json (MECHANISM_PARAMETER, sweep-selected).
# The flat config/synthetic/scenarios/*.yaml files are the v0.1-v0.3 family and are
# left untouched so already-released benchmark versions keep replaying from them.

extends: central_provisional
scenario_id: difficult
display_name: Difficult
version: '0.4'
purpose: 'Harder controlled setting with more identity fragmentation, wording drift, generic/repeated
  text, CPV degradation, and successor timing dispersion.

  '
provenance: SCENARIO_UNIDENTIFIED
references:
- config/synthetic/scenarios/03_buyer_fragmentation.yaml
- config/synthetic/scenarios/05_semantic_drift.yaml
- config/synthetic/scenarios/central_provisional.yaml
baseline_reference: moderate.yaml
scenario_design_note: 'Deliberately imposes stronger recall ceilings and candidate ambiguity than moderate.
  It is a sensitivity/stress scenario, not an empirical estimate of BOAMP recurrence or algorithm performance.

  '
recurrence:
  base_recurrence_propensity: 0.32
  cycle_gap_distribution:
    type: normal
    mean_months: 22.0
    sd_months: 12.0
    min_months: 1.0
  scoped_candidate_environment:
    recurrence_propensity_multiplier: 1.15
    near_window_share: 0.55
    hard_negative_alignment_rate: 0.16
    scoped_buyer_affinity_share: 0.1
    scoped_need_probability_high: 0.78
identifiers:
  siret_missing_rate: 0.84
  siren_only_rate: 0.1
  both_missing_rate: 0.86
  invalid_identifier_rate: 0.02
  wrong_establishment_siret_rate: 0.05
buyer_names:
  false_split_rate: 0.38
  false_merge_rate: 0.12
  generic_collision_rate: 0.48
  persistent_aliases:
    dominant_alias_share: 0.3871
    note: SCENARIO_UNIDENTIFIED. Carries this scenario's v0.3 false_split_rate ratio (0.38 / 0.279 = 1.362)
      onto the v0.4 persistent-alias mechanism, so the required-scenario difficulty gradient is unchanged.
      Only central_provisional/moderate claim observable calibration.
cpv:
  parent_replace_rate: 0.08
  division_only_rate: 0.05
  generic_rate: 0.14
  wrong_related_rate: 0.04
duration:
  rounding_severity: 0.55
  common_admin_value_rate: 0.28
  unit_conversion_failure_rate: 0.035
text:
  next_cycle_drift_severity: 0.72
  hard_negative_overlap_rate: 0.25
  boilerplate_rate: 0.55
  conditional_reuse:
    exact_template_share: 0.88
    near_boilerplate_extra_rate: 0.12
    generic_weak_extra_rate: 0.07
quality_class_mix:
  HIGH: 0.18
  MEDIUM: 0.42
  LOW: 0.4
config_family: v0_4
generated_by: scripts/write_v0_4_scenario_config.py
