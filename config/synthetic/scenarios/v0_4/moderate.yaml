# GENERATED FILE -- do not hand-edit.
# Written by scripts/write_v0_4_scenario_config.py from
# reports/tables/synthetic_benchmark/v0_4_population_alias_revision/calibration/observable_targets.json (EMPIRICAL_OBSERVABLE, calibration buyers only)
# and reports/tables/synthetic_benchmark/v0_4_population_alias_revision/calibration/mechanism_parameters.json (MECHANISM_PARAMETER, sweep-selected).
# The flat config/synthetic/scenarios/*.yaml files are the v0.1-v0.3 family and are
# left untouched so already-released benchmark versions keep replaying from them.

extends: central_provisional
scenario_id: moderate
display_name: Moderate
version: '0.4'
purpose: 'Main BOAMP-like synthetic benchmark scenario for preliminary experiments. Reuses central_provisional''s
  observable calibration and v0.3 candidate environment assumptions under the required scenario name.

  '
provenance: EMPIRICAL_OBSERVABLE, SILVER_STANDARD_APPROXIMATION, SCENARIO_UNIDENTIFIED
references:
- config/synthetic/scenarios/central_provisional.yaml
- config/synthetic/scenarios/02_central_boamp_like.yaml
baseline_reference: central_provisional.yaml
scenario_design_note: 'Moderate is not an empirical estimate of true BOAMP recurrence. It is the central
  scenario assumption under a clearer required name; accepted links, scores, thresholds, and the six-month
  production window are not used as real truth.

  '
config_family: v0_4
generated_by: scripts/write_v0_4_scenario_config.py
buyer_names:
  persistent_aliases:
    dominant_alias_share: 0.55
    note: SCENARIO_UNIDENTIFIED. Carries this scenario's v0.3 false_split_rate ratio (0.279 / 0.279 =
      1.000) onto the v0.4 persistent-alias mechanism, so the required-scenario difficulty gradient is
      unchanged. Only central_provisional/moderate claim observable calibration.
